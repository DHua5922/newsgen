{
  "name": "newsgen-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start -p $PORT",
    "docker-build": "docker build -t newsgen-frontend .",
    "docker-run": "docker run -p 3000:3000 newsgen-frontend",
    "docker-start": "npm run docker-build && npm run docker-run"
  },
  "dependencies": {
    "next": "10.0.5",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
